pipeline{
    //agent any
    agent {
        label 'slave-1'
    }
    environment {
        CON_NAME = 'react-con'
        IMG_NAME = 'sanjai:v1'
    }
    stages{
        stage('Build'){
            steps{
                script{
                    //sh 'sudo su'
                    //sh 'sudo docker stop ${CON_NAME}'
                    //sh 'sudo docker rm -f ${CON_NAME}'
                    //sh 'sudo docker rmi ${IMG_NAME}'
                    sh 'cd /var/jenkins_home/workspace/Sample-react-app/react-app/ ; bash build.sh'
                    
                                   }
            }

        }
        stage('Deploy'){
            steps{
                script{
                    sh 'cd /var/jenkins_home/workspace/Sample-react-app/react-app/ ; bash deploy.sh'

                }
            }
        }
    }
}
